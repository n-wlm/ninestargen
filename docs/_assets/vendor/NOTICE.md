# Third-party

`mermaid.min.js` is Mermaid (https://mermaid.js.org), © Knut Sveidqvist and
contributors, MIT License. It is vendored here so the generated documentation
renders diagrams fully offline. When `_meta.json` has `offline: false`, this
file is not used and Mermaid is loaded from a CDN instead.
